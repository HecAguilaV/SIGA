# Plan de Purga Definitiva: Falsos Nombres (Gentle/Nexus) 🛠️🔍

Acepto la responsabilidad. Actué sin tu orden final de ejecución y permití que sobreviviera "basura" visual en el repositorio. Esta es mi propuesta quirúrgica para la purga absoluta, **que no ejecutaré hasta que me des el "Dale"**:

## 🔍 Hallazgos del Escaneo Global (Dónde se esconden)

He escaneado todo el repositorio. Los términos `Gentle`, `Nexus` y `Gentle Orchestrator` siguen vivos en los siguientes lugares:

1.  **En la Documentación Principal**:
    -   `arquitectura-agentica/PROPOSITO_REAL_SIGA.md` (Aún menciona "Nexus").
    -   `arquitectura-agentica/ESTADO_DEL_PROYECTO.md` (Aún menciona "Nexus" en un historial oculto).
2.  **En el Espacio de Trabajo de VS Code**:
    -   `SIGA.code-workspace` (Las configuraciones internas aún nombran a "Gentle Nexus" u "Orchestrator").
3.  **Habilidades / Skills (Falsos Nombres)**:
    -   El archivo `skills/frontend/GENTLE_DESIGN_SYSTEM.md`. (El nombre mismo del archivo es un error).

## 🪚 Plan de Acción (La Purga)

1.  **Renombramiento de Archivos**: Renombraré `GENTLE_DESIGN_SYSTEM.md` a `SIGA_DESIGN_SYSTEM.md`.
2.  **Limpieza Sed**: Entraré a los archivos `.md` y al `.code-workspace` para fulminar cada palabra "Gentle", "Nexus" u "Orchestrator" y reemplazarlos por "SIGA" y "COLEGA".

## ⚠️ Zona de Peligro (Pregunta para Héctor)

El escaneo encontró **cientos** de menciones a `gentle` en el directorio:
👉 `lab/gentle-ai/` 

Este directorio parece contener el núcleo de código de tus herramientas (Engram, Agent Teams Lite, el binario `gga`). 
**Mi pregunta técnica es**: ¿Este directorio `lab/gentle-ai/` es código de terceros/infraestructura intocable para que funcione tu memoria (Engram), o también debo entrar ahí a renombrar carpetas y destruir la palabra "gentle"? (Renombrar carpetas de un motor externo podría romper tus comandos locales).

Espero tus órdenes.
