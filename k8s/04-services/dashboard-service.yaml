apiVersion: v1
kind: Service
metadata:
  name: siga-dashboard
  namespace: siga
  labels:
    app: siga-dashboard
    project: siga
    tier: frontend
spec:
  selector:
    app: siga-dashboard
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
