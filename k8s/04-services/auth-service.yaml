apiVersion: v1
kind: Service
metadata:
  name: siga-auth
  namespace: siga
  labels:
    app: siga-auth
    project: siga
    tier: backend
spec:
  selector:
    app: siga-auth
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      name: http
  type: ClusterIP
